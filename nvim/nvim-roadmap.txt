nvim roadmap:

- improve font & general look-n-feel:
	- bolder font;
	- better contrast with highlighting;
	- more vivid colors(like intellij);
- map <leader>ft to "find test file";

- have topbar display the complete filepath;
- add https://github.com/stevearc/stickybuf.nvim plugin;
- add symbols explorer plugin(aerial):
    - as a popup, just like Idea's FileStructurePopup;
    - make sure you can type to filter without additional keystrokes;
    - nice to have: colorful icons to distinguished between private & public;

- figure out how to have project-specific configs
- Add mapping("live template") for `|> IO.inspect(label: $SELECTION)`

- add plugin to run custom scripts(https://github.com/stevearc/overseer.nvim)

- add a file explorer plugin;
- explore & customize telescope;
- add sneak plugin;
- add matchit plugin;
- add a plugin for scratch files;
- add a plugin to connect to Postgres;
- figure out find/replace within buffer:
	- for simple cases, <C-g> is now doing the job;
- figure out find/replace across files(quickfix list);
